name: rename_test

on:
  pull_request:

permissions:
  contents: read

jobs:
  rename_test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-node@v5
      with:
        node-version: 22
    - run: npm ci
    - run: npm run rename
